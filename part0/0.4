Diagram representing the chain of events caused by creating a new note and clicking submit button.

```mermaid
SequenceDiagram
  participant browser
  participant server
  
  browser ->>server: GET


```
